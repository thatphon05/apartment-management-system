Run Composer update

```sh
composer update
```

Run Migration

```sh
php artisan migrate
```

Run Seeder

```sh
php artisan db:seed
```

Refresh the database and run all database seeds...

```sh
php artisan migrate:refresh --seed
```
